name: helpdex
description: A new Flutter project.
  
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2  # Update this line to the suggested version

dev_dependencies:
  flutter_launcher_icons: ^0.13.1  # Check for the latest version
  rename_app: ^1.6.1  # Updated to a compatible version

flutter_icons:
  android: true
  ios: true
  image_path: "assets/img/icon/dswd bear-removebg.png"  # Updated path to remove spaces

flutter:
  uses-material-design: true

  assets:
    - assets/img/bear/dswd bear-removebg.png  # Updated path to remove spaces
    - assets/img/dswd/dswd-fo-logo.png
